import requests
import time
import json

def test():
    token = ""
    with open('/Users/mac/Desktop/phhh/frontend/.env.local', 'r') as f:
        pass # just need to grab a token to test the API

import os
# Since I can't easily grab a token from the frontend, I'll test the raw Supabase latency on the VPS instead
