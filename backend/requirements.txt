fastapi==0.100.1
uvicorn[standard]==0.23.2
pydantic==1.10.13
email-validator==2.1.0
PyJWT==2.8.0
passlib==1.7.4
bcrypt==4.0.1
python-multipart==0.0.6
supabase==2.9.0
# mistralai removed - using direct HTTP calls instead to avoid httpx conflict
python-docx==1.1.0
aiofiles==23.2.1
# httpx will be auto-resolved by supabase and mistralai
psycopg2-binary==2.9.9
setuptools>=65.0.0
wheel>=0.37.0
# LangChain and related dependencies - using compatible versions
langchain>=0.1.0,<0.2.0
langchain-community>=0.0.10,<0.3.0
langchain-core>=0.1.28,<0.2.0
langchain-text-splitters>=0.0.1,<0.1.0
# Document processing
pypdf==3.17.4
PyPDF2==3.0.1
docx2txt==0.8
Pillow==10.4.0  # For image processing
# Caching and utilities
cachetools==5.3.2
# Cohere for embeddings - use httpx directly to avoid sagemaker conflicts
# cohere==5.11.0  # Conflicts with pydantic 1.x
# We'll use direct HTTP calls instead